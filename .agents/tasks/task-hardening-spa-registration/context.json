{
  "project_type": "Full-stack TypeScript portal (Vite React SPA + Express API + Python FastAPI agents)",
  "language": "TypeScript (frontend/backend), Python (ADK agents)",
  "build_system": "npm + vite + esbuild; Docker multi-stage build; Vercel for deployment",
  "test_framework": "No test files found (no test runner configured)",
  "build_command": "source /root/.nvm/nvm.sh && npm run build",
  "test_command": "source /root/.nvm/nvm.sh && npm run lint",
  "verification_instructions": "1) npm run lint (tsc --noEmit) must pass. 2) npm run build (vite build + esbuild server.ts) must pass. 3) Verify vercel.json SPA rewrites include catch-all. 4) Confirm no hardcoded admin/Admin@2024 password.",
  "snapshot_or_generated_files": null,
  "setup_instructions": "source /root/.nvm/nvm.sh && npm install",
  "environment_constraints": "OPEN_INTERNET network. Node via nvm (source /root/.nvm/nvm.sh). Resend sandbox only sends to omkardsupe143644@gmail.com. No WhatsApp Business API credentials available. Docker daemon should be available for docker build.",
  "contribution_requirements": "Branch: feat/emerald-email-otp. Commit meaningful groups. Push and deploy to Vercel.",
  "key_patterns": "Express app assembled in server/app.ts from modular route registrators. Security middleware in server/security.ts (HMAC sessions, rate limits, concurrency gates). Auth flow: password verify -> OTP challenge -> verify OTP -> session cookie. Supabase accessed via service_role key with RPCs for atomic ops. Frontend uses AppContext for auth state, LoginPage for auth UI. Vercel deploys SPA from dist/ with api/ serverless function.",
  "relevant_files": [
    "vercel.json",
    "server/auth.ts",
    "server/security.ts",
    "server/app.ts",
    "server/agents.ts",
    "server/data.ts",
    "server.ts",
    "api/index.ts",
    "src/App.tsx",
    "src/components/LoginPage.tsx",
    "src/contexts/AppContext.tsx",
    "supabase/migrations/202608180002_atomic_email_otp.sql",
    "Dockerfile",
    "docker-compose.yml",
    "k8s/deployment.yaml",
    "k8s/hpa.yaml",
    "package.json"
  ],
  "directory_structure": "Root: server/ (Express backend modules), src/ (React SPA), api/ (Vercel serverless), agents/ (Python ADK), k8s/ (Kubernetes manifests), supabase/migrations/ (DB schema), dist/ (build output)"
}
